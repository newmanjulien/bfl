# Convex CLI target used by `convex dev`, `convex deploy`, and codegen.
CONVEX_DEPLOYMENT=

# Convex's SvelteKit integration manages these URLs in local `.env.local`.
PUBLIC_CONVEX_URL=
PUBLIC_CONVEX_SITE_URL=
